#!/bin/rc

. /usr/local/libexec/rc/read
dir = `(dirname $0)
pkt = $dir/pkt

fn check {
  python3 $dir/check.py $pkt/$1
  echo 'Press ENTER to continue or ESC to stop...'
  key = `read
  ~ $key () && exit
}

for (f in `{ ls $pkt | grep '^[0-9]\+$' })
  check $f

exit 0
