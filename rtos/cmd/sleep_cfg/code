# TODO: incomplete

~ packet.pio == FF:
  pin = *C000126C
  ~ pin != FF && pin != FE && gpio_init(pin, false)
  *C0001268 = FFFFFFFF
  *C001B245 = 0  # byte
  *C001B24C = 0  # byte
  exit

*C0001268 = packet.gpio
*C000126D = ?
sleep_memcpy_C001B244
*C001B245 = 1

pin = ?
~ pin != FF && pin != FE:
  gpio_init(pin, true)
  gpio_set_output(pin, true)
