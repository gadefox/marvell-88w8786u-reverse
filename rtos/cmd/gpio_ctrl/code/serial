# gpio_control_serialize

# first packet
packet = (packet)buf
packet.task = 8     # byte
packet.action = 1   # byte
packet.size = 6     # word
buf += 4
# +4 bytes

for led_i in 0..2:
  led = leds[led_i]
  state = led.states[0]
  packet.payload[led_i * 2] = state.lednum
  packet.payload[led_i * 2 + 1] = state.gpio
  buf += 2
# +6 bytes

for led_i in 0..2:
  led = leds[led_i]

  for state_i in 0..8:
    state = led.states[state_i]

    # next packet
    packet = (packet)buf
    packet.task = 9    # byte
    packet.action = 1  # byte
    packet.size = 4    # word
    packet.payload[0] = state_i
    packet.payload[1] = state.lednum
    packet.payload[2] = state.behavior
    packet.payload[3] = state.config
    buf += 8
  # +72 bytes
# +216 bytes

packet = (packet)buf
packet.task = FF    # byte
packet.action = FF  # byte
packet.size = 0     # word
# +4 bytes

# total: 230 bytes
