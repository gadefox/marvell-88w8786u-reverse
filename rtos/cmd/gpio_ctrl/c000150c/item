+00 byte block      (0..2)
+01 byte gpio       (0..15); 18 is used for input
+02 byte case       switch table index; see `code/dispatch`
+03 byte cfg        config for GPIO registers (e.g. 9000024C)
----
 04 sizeof
