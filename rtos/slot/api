0000EB4C pool_slot_alloc(pool, uint size, uint flags)             see `alloc`
0000EBDA pool_slot_free(slot)                                     see `free`
0000EC22 pool_slot_num_allocated(pool)                            see `numalloc`
0000EC50 pool_slot_get_total_capacity(pool)                       see `capacity`
0000EC78 pool_slot_count_by_tag(pool, uint tag)                   see `tag`
0000ECAE pool_slot_empty(pool)                                    see `empty`
000170D0 pool_slot_init(pool, cfg[] configs, ptr[] slots, ptr descs); see `init`
